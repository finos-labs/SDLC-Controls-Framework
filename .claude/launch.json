{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "jekyll",
      "runtimeExecutable": "bash",
      "runtimeArgs": ["-c", "docker run --rm -p 4000:4000 -v ./docs:/srv/jekyll jekyll/jekyll:4 jekyll serve --host 0.0.0.0 --port 4000 --config _config.yml,_config_dev.yml"],
      "port": 4000
    }
  ]
}
